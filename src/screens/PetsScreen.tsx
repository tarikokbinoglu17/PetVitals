import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { demoPets } from '../data/demo';
import { colors, shadow } from '../theme';

export function PetsScreen() { return <View style={styles.page}><Text style={styles.title}>Dostlarım</Text><Text style={styles.sub}>Tüm sağlık bilgileri tek yerde.</Text>{demoPets.map((pet, index) => <View key={pet.id} style={styles.card}><View style={[styles.avatar, { backgroundColor: index ? '#FDE8DF' : colors.primarySoft }]}><Text style={styles.emoji}>{pet.species === 'Kedi' ? '🐱' : '🐶'}</Text></View><View style={styles.info}><Text style={styles.name}>{pet.name}</Text><Text style={styles.meta}>{pet.breed} • {pet.weight} kg</Text><Text style={styles.birth}>Doğum: {new Date(pet.birthDate).toLocaleDateString('tr-TR')}</Text></View><Text style={styles.arrow}>›</Text></View>)}<View style={styles.add}><Text style={styles.addText}>＋ Yeni dost ekle</Text></View></View>; }
const styles = StyleSheet.create({ page: { padding: 22 }, title: { color: colors.text, fontSize: 30, fontWeight: '900' }, sub: { color: colors.muted, marginBottom: 22, marginTop: 5 }, card: { ...shadow, alignItems: 'center', backgroundColor: colors.surface, borderRadius: 20, flexDirection: 'row', marginBottom: 14, padding: 16 }, avatar: { alignItems: 'center', borderRadius: 28, height: 56, justifyContent: 'center', width: 56 }, emoji: { fontSize: 28 }, info: { flex: 1, marginLeft: 14 }, name: { color: colors.text, fontSize: 19, fontWeight: '800' }, meta: { color: colors.muted, marginTop: 4 }, birth: { color: colors.primary, fontSize: 12, fontWeight: '700', marginTop: 5 }, arrow: { color: colors.muted, fontSize: 30 }, add: { alignItems: 'center', borderColor: colors.primary, borderRadius: 18, borderStyle: 'dashed', borderWidth: 1.5, marginTop: 5, padding: 18 }, addText: { color: colors.primary, fontWeight: '800' } });

